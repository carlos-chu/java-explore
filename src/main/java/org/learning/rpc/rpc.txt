RPC原理总结：
	1.rpc是为了远程调用服务而生的，为什么不用http服务，首先影响效率，http服务需要附加好多状态信息，再者他是短连接，网络影响大，其次参数需要经过servlet的解析
	2.rpc分为客户端和服务端，服务端提供服务，客户端负责调用服务，其中为了使用方便，好像本地调用一样的使用远程服务，rpc使用了动态代理包装了远程调用服务接口，这样可以
	      在本地封装网络传输的逻辑，把远程调用服务的参数包装成协议（xml,json,bin），再进行网络传输，网络传输必定要进行序列化和反序列化